const vec3 WORLD_FORWARD = vec3(1.0, 0.0, 0.0);
const vec3 WORLD_RIGHT = vec3(0.0, 1.0, 0.0);
const vec3 WORLD_UP = vec3(0.0, 0.0, 1.0);

const float PI = 3.141592653589793;
const float TAU = PI * 2.0;
const float EPSILON = 0.0001;
